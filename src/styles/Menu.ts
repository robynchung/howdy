import { styled } from "@mui/system";

export const StyledFloatContainer = styled("div")({
  position: "absolute",
  top: 20,
  right: 20,
  cursor: "pointer",
  display: "flex",
  alignItems: "center",
});
